<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codeeaters.elitecard">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MapActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.EnregistrementAapeActivity"
            android:label="Enregistrer un nouveau AAPE"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.EnregistrerAapeSuiteActivity"
            android:label="Enregistrer un nouveau AAPE"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ListeVisitesActivity"
            android:label="Visites au programme"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".Apropos"
            android:label="@string/title_activity_apropos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Aide"
            android:label="@string/title_activity_aide"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.VisiteFloraisonActivity"
            android:label="Visite Floraison"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.VisiteFructificationActivity"
            android:label="Visite Fructification"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.VisiteRecoltesActivity"
            android:label="Visite Recoltes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.VisiteRendementsActivity"
            android:label="Rendements"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.PlaningVisitesActivity"
            android:label="Planing dees Visites"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Maladies" />
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/title_activity_login"/>
        <activity
            android:name=".activity.ListProducteursActivity"
            android:label="Liste des producteurs"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ListArbresActivity"
            android:label="Liste des arbres"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>